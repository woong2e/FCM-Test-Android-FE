import React from 'react';
import { Text, View } from "react-native"

const LoginScreen = () => {
    return <View style={{flex: 1, alignItems: 'center', justifyContent: 'center'}}>
        <Text style={{ fontSize: 50 }}>{'LoginScreen'}</Text></View>
}

export default LoginScreen